// @flow

import React from 'react';

import './profile-photo.scss';

export const ProfilePhoto = (props: any) => (
  // eslint-disable-next-line jsx-a11y/alt-text
  <img className="post__profile-photo" {...props} />
);
